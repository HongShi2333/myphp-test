{
  "build": {
    "env": {
      "PHP_VERSION": "7.4" // 替换为你使用的 PHP 版本
    },
    "commands": [
      {
        "command": "echo 'extension=curl.so' >> $PHP_INI_DIR/conf.d/vercel.ini"
      }
    ]
  }
}
